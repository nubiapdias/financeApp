import styled from "styled-components"

export const Container = styled.div `
    max-width: 70rem;
    width: 98%;
    margin: 0 auto;
    display: flex;
    gap: 1.25rem;
    margin-top: -50px;
    justify-content: space-around;
`;